{% extends 'base.html.twig' %}

{% block title %}OrderLog{% endblock %}

{% block body %}
    <h1>OrderLog</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ order_log.id }}</td>
            </tr>
            <tr>
                <th>Quantity</th>
                <td>{{ order_log.quantity }}</td>
            </tr>
            <tr>
                <th>BuyingPrice</th>
                <td>{{ order_log.buyingPrice }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('order_log_index') }}">back to list</a>

    <a href="{{ path('order_log_edit', {'id': order_log.id}) }}">edit</a>

    {{ include('order_log/_delete_form.html.twig') }}
{% endblock %}
