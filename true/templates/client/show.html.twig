{% extends 'base-admin.html.twig' %}

{% block title %}Client{% endblock %}

{% block body %}
    {{ parent() }}
    <div class="admin-page_content">
        <h1>Client</h1>
        <table class="table">
            <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ client.id }}</td>
                </tr>
                <tr>
                    <th>Email</th>
                    <td>{{ client.email }}</td>
                </tr>
                <tr>
                    <th>Roles</th>
                    <td>{{ client.roles ? client.roles|json_encode : '' }}</td>
                </tr>
                <tr>
                    <th>Password</th>
                    <td>{{ client.password }}</td>
                </tr>
                <tr>
                    <th>ClientName</th>
                    <td>{{ client.clientName }}</td>
                </tr>
                <tr>
                    <th>ClientFirstname</th>
                    <td>{{ client.clientFirstname }}</td>
                </tr>
                <tr>
                    <th>Genre</th>
                    <td>{{ client.genre ? 'Yes' : 'No' }}</td>
                </tr>
                <tr>
                    <th>ClientDob</th>
                    <td>{{ client.clientDob ? client.clientDob|date('Y-m-d') : '' }}</td>
                </tr>
            </tbody>
        </table>
            <table class="table">
            <tbody>
                <tr>
                    <th>numéro</th>
                    <td>{{ adresse.number }}</td>
                </tr>
                <tr>
                    <th>Rue</th>
                    <td>{{ adresse.Line1 }} <br> {{ adresse.Line2}} <br> {{ adresse.Line3}}</td>
                </tr>
                <tr>
                    <th>Code</th>
                    <td>{{ adresse.code }}</td>
                </tr>
                <tr>
                    <th>Ville</th>
                    <td>{{ adresse.location }}</td>
                </tr>
            </tbody>
        </table>
        
        <a href="{{ path('client_index') }}">back to list</a>
        <a href="{{ path('client_edit', {'id': client.id}) }}">edit</a>
        {{ include('client/_delete_form.html.twig') }}
    </div>
{% endblock %}
